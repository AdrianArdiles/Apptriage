# URL base de la API en producción (web Vercel y APK).
# En Vercel: configurar también en Project → Settings → Environment Variables.
# La APK (Capacitor) usa esta URL al compilar con npm run build:android.
NEXT_PUBLIC_API_URL=https://apptriage.vercel.app
